function increment() {
  counter++;
  return counter;
}

// Auth module setup
console.log("User fetched", user);
console.warn("Missing field");

const result = processData(input);

// TODO: remove after QA validation
const debug = true;
